import {makeStyles} from '@mui/styles';

const detailPageStyles = makeStyles(theme => ({
  boxStyles: {
    margin: "auto",
    maxWidth: "800px",
    backgroundColor: "white",
    boxShadow: "rgb(149 157 165 / 20%) 0px 0px 30px 24px !important",
    borderRadius: "10px",
    // overflow: "hidden"
  },
}));

export default detailPageStyles;
