import React from 'react';
import {Box, CircularProgress} from '@mui/material';

const PreLoader = () => {
  return (
    <Box>
      <CircularProgress/>
    </Box>
  );
};

export default PreLoader;
