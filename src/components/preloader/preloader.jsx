import React from 'react';
import { CircularProgress, Box } from '@mui/material';

const PreLoader = () => {
  return (
    <Box>
      <CircularProgress />
    </Box>
  );
};

export default PreLoader;
