import {makeStyles} from '@mui/styles';

const homeComponentStyles = makeStyles(theme => ({
  link: {
    textDecoration: 'none',
    color: 'black !important',
  },
}));

export default homeComponentStyles;
